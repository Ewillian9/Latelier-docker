<turbo-frame id="message_form">
    {{ form_start(form, {'attr': {'class': 'mt-6'}}) }}
        {{ form_widget(form) }}
        <button class="px-3 py-1 rounded bg-green-300 dark:bg-green-700 hover:bg-green-400 hover:dark:bg-green-600 text-sm inline-block transition">{{'send'|trans}}</button>
    {{ form_end(form) }}
</turbo-frame>