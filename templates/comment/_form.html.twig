<turbo-frame id="message_form">
    {{ form_start(form, {'attr': {'class': 'mt-6'}}) }}
        {{ form_widget(form) }}
        <button class="flex items-center gap-1 px-3 py-1 rounded bg-green-300 dark:bg-green-700 hover:bg-green-400 hover:dark:bg-green-600 text-sm transition">{{ ux_icon('ic:outline-send', { class: 'w-5 h-5' }) }}<span class="hidden md:inline">{{'send'|trans}}</span></button>
    {{ form_end(form) }}
</turbo-frame>